export {
  activeCommand,
  backspace,
  disarm,
  emptyStack,
  escape,
  projectOf,
  pushCommand,
  pushProject,
  pushSpace,
  scopeOf,
  spaceOf,
  stackOf,
} from './stack';
export type { CommandSegment, ProjectSegment, Scope, Segment, SpaceSegment, Stack } from './types';
